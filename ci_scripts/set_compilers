export MPICC=`which mpicc`
export MPICXX=`which mpicxx`
export CC=`which gcc`
export CXX=`which g++`
export CCACHE_COMPILERCHECK=content
${CC} --version
${CXX} --version

export CMAKE_ROOT=${HOME}/install/cmake
export PATH=${CMAKE_ROOT}/bin:${PATH}
